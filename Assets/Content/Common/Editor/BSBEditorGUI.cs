using UnityEngine;
using UnityEditor;
using System.Collections;
using BSB;

namespace BSB
{

	public static class BSBEditorGUI 
	{
		

		public static void ReservesField(Rect position, SerializedProperty property)
		{
	/*		var rect = position;

		//	rect.y += rect.height;
			_MeansField(rect, reserves.funds, "Funds");
			rect.y += rect.height;
			_MeansField(rect, reserves.materials, "Materials");
			rect.y += rect.height;
			_MeansField(rect, reserves.workers, "Workers");
	*/	}
		
	/*	private static void _MeansField(Rect rect, IBSBReserveMeans means, string label)
		{
			means.Set(
				EditorGUI.IntField(rect, new GUIContent(label), means.amount));
		}
		*/
	}

}
